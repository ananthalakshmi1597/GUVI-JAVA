#include<stdio.h>
#include<conio.h>
int main()
{
int n;
scanf("%d",&n);
if(n!=0)
{
   if(n>0)
     {
        printf("%d",positive);
     }
     else
        printf("%d",negative);
}
else
    printf("%d",zero);
return 0;
}
